interface:
  display_name: "API Conventions"
  short_description: "Build Carta file routes and inherited scopes"
  default_prompt: "Use $api-conventions when adding or changing Carta backend file routes, scopes, entities, or services."
